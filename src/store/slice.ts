import { createSlice } from "@reduxjs/toolkit";


const paradiseSlice = createSlice({
  name: "paradiseapp",
  initialState: {
  },
  reducers: {
  }
});

export const {} = paradiseSlice.actions;

export default paradiseSlice.reducer;